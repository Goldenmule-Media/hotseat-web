{
  "name": "structured-wiki-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "wiki",
    "wiki-server"
  ],
  "scripts": {
    "typecheck": "npm run typecheck -w wiki -w wiki-server",
    "test": "npm run test -w wiki -w wiki-server",
    "build": "npm run build -w wiki -w wiki-server"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "tsx": "^4.19.2",
    "@types/node": "^24.0.0"
  }
}
