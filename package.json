{
  "name": "structured-wiki-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "wiki",
    "wiki-server",
    "wiki-mcp"
  ],
  "scripts": {
    "typecheck": "npm run typecheck -w wiki -w wiki-server -w wiki-mcp",
    "test": "npm run test -w wiki -w wiki-server -w wiki-mcp",
    "build": "npm run build -w wiki -w wiki-server -w wiki-mcp"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "tsx": "^4.19.2",
    "tsup": "^8.5.0",
    "@types/node": "^24.0.0"
  }
}
