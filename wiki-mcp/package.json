{
  "name": "wiki-mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "Long-lived CQRS read model + MCP server for the wiki engine. Embeds `wiki`, keeps it hydrated, and maintains a durable SQL read model (Kysely · PGlite/pg). Hosted by wiki-server.",
  "exports": {
    ".": "./src/main.ts"
  },
  "bin": {
    "wiki-mcp": "dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsdown",
    "start": "tsx src/bin.ts",
    "backfill-titles": "tsx src/backfill-titles.ts",
    "backfill-serials": "tsx src/backfill-serials.ts",
    "migrate-workspace": "tsx src/migrate-workspace.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "wiki": "*",
    "kysely": "^0.27.5",
    "@durable-streams/client": "^0.2.6",
    "@electric-sql/pglite": "^0.2.17",
    "pg": "^8.13.1",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10"
  }
}
